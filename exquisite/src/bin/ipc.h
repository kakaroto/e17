typedef enum _Ipc_Major
{
     EXIT_NOW,
     EXIT,
     TITLE,
     MESSAGE,
     PROGRESS,
     TICK,
     PULSATE,
     TEXT,
     TEXT_URGENT,
     STATUS,
     SUCCESS,
     FAILURE,
     CLEAR,
     TIMEOUT
} Ipc_Major;
