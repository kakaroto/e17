SUBDIRS = src data

ACLOCAL_AMFLAGS = -I m4

EXTRA_DIST = config.rpath m4/ChangeLog
MAINTAINERCLEANFILES = Makefile.in config.guess config.h.in aclocal.m4 config.h.in~ config.sub configure depcomp install-sh missing config.rpath mkinstalldirs
