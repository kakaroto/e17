Installation instructions:

1) ./configure --prefix=PREFIX

Replace PREFIX with prefix you want, for example /usr, /opt, or /usr/local.

2) make

3) su root

Not needed when you are installing it to .local in your home folder for example.

4) make install
