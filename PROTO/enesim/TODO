+ Split the 'host cpu' implementatino into another library, mainly to add support
  there for different cpus.
+ Remove the primitives
+ What to do with the extender "classes", place them into eina?
+ Move every error to eina's error
