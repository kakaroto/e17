
#include "EMap.h"

EMap_Track *emap_track_gpx_new(const char *file)
{
   //check file

   EMap_Track *track = emap_track_new(EMAP_TRACK_TYPE_GPX);

   //open file
   //parse file
   //..

   return track;
}
