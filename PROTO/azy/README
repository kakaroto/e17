Azy is a full rewrite of libzxr, itself a modification of libxr.
It provides functions for manipulating RPC clients/servers as well as RSS feeds.

Dependencies:
eina
ecore

Install:
Simply run bootstrap.sh to configure and compile the code, then run make install

Tests:
run 'make check' to compile the tests; more to come on this
