Azy is a full rewrite of libzxr, itself a modification of libxr.  It is meant for
implementing rpc clients and servers in a simple manner.

Dependencies:
eina
ecore
libxml2

Install:
Simply run bootstrap.sh to configure and compile the code, then run make install

Tests:
run 'make check' to compile the tests; more to come on this
