program { name: "start_update_timer"; signal: "load"; source: "";
	script {
            resync_seconds(0);
            resync_time(0);
        }
    }

program { name, "public_announcement"; signal, "load"; source, "";
	script {
	     emit("tic toc..", "");
	}
    }
