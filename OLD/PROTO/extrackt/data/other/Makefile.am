MAINTAINERCLEANFILES = \
extrackt.desktop

icondir = $(prefix)/share/pixmaps
icon_DATA = extrackt.png

appdir = $(prefix)/share/applications
app_DATA = extrackt.desktop

EXTRA_DIST = extrackt.png extrackt.desktop.in extrackt.desktop
