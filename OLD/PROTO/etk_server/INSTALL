Installation:
=============
./autogen.sh
./configure
make
make install

You will need Etk's source code in the same place as when it was compiled.

Note:
=====
BSD systems can use configgen.sh from their system and gmake.
