
#include "menu.h"

#if 0
#include "e.h"
#endif

#include "menuedit.h"


void
med_edit_menu_item(E_Menu *m, E_Menu_Item *mi, void *data)
{

#if 0
  printf( "text: %s, exe: %s, icon: %s, # %d,%d.\n", 
	  mi->str, 
	  (char*)data,
	  mi->icon, mi->db_num1, mi->db_num2);

  med_entry_text( mi->str, (char*)data, mi->icon );
#endif
}


/*eof*/
