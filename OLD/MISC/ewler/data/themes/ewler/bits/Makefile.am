SUBDIRS = images

EDCS = selected-groups.edc selected-programs.edc \
			 background-groups.edc background-images.edc

EETS = $(EDCS:%.edc=%.eet)

EXTRA_DIST = $(EDCS)
CLEANFILES = $(EETS)

themedir = $(pkgdatadir)/themes/default/bits
