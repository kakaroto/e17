
To generate html output from efsd-manual.sgml, you can use e.g.
James Clark's jade. You need to have the HTML DocBook stylesheets
installed. Then run:

jade -t sgml -d PATH/TO/docbook.dsl efsd-manual.sgml

On a Debian system, the path to docbook.dsl is
/usr/lib/sgml/stylesheet/dsssl/docbook/nwalsh/html/docbook.dsl

