SUBDIRS = src

MAINTAINERCLEANFILES = Makefile.in aclocal.m4 config.guess \
                       config.log config.sub configure depcomp \
		       install-sh ltmain.sh missing \
		       edbus-config

bin_SCRIPTS = edbus-config
