programs {
    program {
        name, "normal";
        signal, "default";
        source, "*";
        action, STATE_SET "default" 0.0;
        transition, LINEAR 0.0;
        target, "dot";
    }
          
    program {
        name, "checked";
        signal, "checked";
        source, "*";
        action, STATE_SET "checked" 0.0;
        transition, LINEAR 0.0;
        target, "dot";
    }
}

