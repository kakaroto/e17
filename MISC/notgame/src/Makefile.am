# $Id$

bin_PROGRAMS = notgame

notgame_SOURCES = conf.c dest.c help.c notgame.c parse.c play.c players.c pregame.c strings.c

INCLUDES = -I. -I../include -I.. -I$(includedir) -I$(prefix)/include
LDADD = -L$(libdir) -L$(prefix)/lib

EXTRA_DIST = conf.h debug.h dest.h help.h notgame.h parse.h play.h players.h pregame.h strings.h
