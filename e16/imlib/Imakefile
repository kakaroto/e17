#if (ProjectX < 5)
includes::
        @echo
        @echo "Imlib requires the XShm extension, which usually isn't present"
        @echo "in X11R4 or lower...but, we'll try to build anyway..."
        @echo
        @echo "[If your X server *really* is X11R5 or higher, then your"
        @echo "Project.tmpl is broken.  Contact your system administrator.]"
        @echo ""
        @sleep 10
#endif

#define IHaveSubdirs
SUBDIRS = Imlib gdk_imlib config utils

#include "cf/ImlibPre.tmpl"

#include "cf/ImlibPost.tmpl"
