XCOMM  configuration file Imakefile for imlib
XCOMM  --------------------------------------

XCOMM  Grab our defaults...

#include "../cf/ImlibPre.tmpl"

XCOMM  This is a _really_ sloppy way to do this, but we can't use
XCOMM  .SUFFIX rules in case im_palette.pal suddenly gets created
XCOMM  from configure in the future.
imrc:  imrc.in
       cat imrc.in | sed -e 's#@prefix@/etc#$(CNFDIR)#g' > imrc

im_palette.pal:

InstallNonExecFile(imrc,$(CNFDIR))
InstallNonExecFile(im_palette.pal,$(CNFDIR))

#include "../cf/ImlibPost.tmpl"
