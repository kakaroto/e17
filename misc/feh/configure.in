dnl Process this file with autoconf to create configure.

AC_INIT(src/main.c)
AM_INIT_AUTOMAKE(feh, 0.6.2)

AC_PROG_CC
AM_PROG_CC_STDC
AC_C_CONST

AC_PROG_INSTALL
AC_PROG_MAKE_SET

dnl I'm only checking for imlib2. If you have this installed, you prolly
dnl have the libs it needs.
AC_CHECK_LIB(Imlib2, imlib_load_image, , [
echo "ERROR:  You need libImlib2 to build feh.";
             AC_MSG_ERROR([Fatal:  libImlib2 not found.])], -lm)
LIBS="$LIBS -lm"

echo ""
if test -z "$HUMOR"; then
  echo "Checking for pr0n in $HOME"
  echo "."
  echo ".."
  echo "..."
  echo ".... OMG, there's *tons*"
  echo "It looks like you need feh :)"
  echo ""
fi

AC_OUTPUT(Makefile \
feh.spec \
src/Makefile \
src/ttfonts/Makefile \
)
