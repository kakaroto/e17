Compile:

$ ./configure
$ make
$ su -c "make install"

(Red hat users, use $ ./configure --prefix=/usr)

Run the test scripts in the src directory to see it in action.


What is feh?

http://www.feh.org/wordfeh.htm
