## Process this file with automake to produce Makefile.in

SUBDIRS = giblib

docs_DATA = README AUTHORS ChangeLog TODO
docsdir = $(prefix)/doc/giblib

EXTRA_DIST= giblib.spec $(docs_DATA)

bin_SCRIPTS = giblib-config
